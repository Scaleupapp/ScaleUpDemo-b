name: Deploy to EC2

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Backend to EC2
    runs-on: ubuntu-latest

    steps:
      - name: Fix divergent branches and deploy
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/ubuntu/ScaleUpDemo-b
            git fetch origin
            git reset --hard origin/master
            cd /home/ubuntu
            ./deploy.sh

      - name: Health Check
        run: |
          sleep 5
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://${{ secrets.EC2_HOST }}:5000/health)
          if [ "$HTTP_STATUS" = "200" ]; then
            echo "✅ Deployment successful! Health check passed."
          else
            echo "❌ Health check failed with status: $HTTP_STATUS"
            exit 1
          fi
